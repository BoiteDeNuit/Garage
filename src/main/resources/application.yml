spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/garage
    username: garage
    password: secret
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true